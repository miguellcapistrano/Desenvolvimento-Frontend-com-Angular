const miguell = {
    name: "Miguell",
    idade: 30,

    descrever: function() {
        console.log(`Meu nome é ${this.name} e minha idade é ${this.idade} anos.`);
    }
};

const atributo = "idade"
console.log(miguell.idade);
